<header class="site-header" data-header>
  <div class="container">
    <div class="header-inner">
      <button class="mobile-toggle" aria-label="Toggle menu" data-menu-toggle>
        <span></span>
        <span></span>
        <span></span>
      </button>
      
      <a href="{{ store.url }}" class="logo">
        {% if settings.logo %}
          <img src="{{ settings.logo | img_url: '200x' }}" alt="{{ shop.name }}" width="120" height="40" loading="eager">
        {% else %}
          <span class="logo-text">{{ shop.name }}</span>
        {% endif %}
      </a>
      
      <nav class="main-nav" data-nav>
        <ul class="nav-list">
          {% for link in linklists.main-menu.links %}
            <li class="nav-item{% if link.active %} active{% endif %}">
              <a href="{{ link.url }}" class="nav-link">{{ link.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </nav>
      
      <div class="header-actions">
        <a href="{{ store.url }}/search" class="action-btn" aria-label="Search">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <circle cx="11" cy="11" r="8"></circle>
            <path d="m21 21-4.35-4.35"></path>
          </svg>
        </a>
        
        <a href="{{ store.url }}/account" class="action-btn" aria-label="Account">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
        </a>
        
        <a href="{{ store.url }}/cart" class="action-btn cart-btn" aria-label="Cart" data-cart-toggle>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"></path>
            <path d="M3 6h18"></path>
            <path d="M16 10a4 4 0 0 1-8 0"></path>
          </svg>
          <span class="cart-count" data-cart-count>{{ cart.item_count }}</span>
        </a>
      </div>
    </div>
  </div>
</header>

<div class="mobile-overlay" data-overlay></div>
